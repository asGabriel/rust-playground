pub fn variables_operations() {
    // Operações com variáveis
    // 1. Declare duas variáveis inteiras, `a` e `b`, e atribua a elas os valores `10` e `20`, respectivamente.
    // 2. Calcule a soma de `a` e `b` e imprima o resultado.
    // 3. Declare uma variável inteira `contador` e inicialize-a com `0`.
    // 4. Incremente o valor de `contador` em `1` e imprima o resultado.
    // Coloque suas operações com variáveis aqui:
    let a = 10;
    let b = 20;

    let soma = a * b;

    let mut cont = 0;
    cont += 1;
    // Impressão dos resultados
    println!("Sum of a and b: {}", soma);
    println!("Cont: {}", cont);
}
