pub fn initializing_variables() {
    // Declaração de variáveis
    // 1. Declare uma variável inteira chamada `idade` e atribua a ela o valor `30`.
    // 2. Declare uma variável de ponto flutuante chamada `altura` e atribua a ela o valor `1.75`.
    // Coloque suas declarações de variáveis aqui:

    let age = 30;
    let height = 1.50;

    // Impressão das variáveis
    println!("The age is {}, and heigth is {}", age, height)
}
